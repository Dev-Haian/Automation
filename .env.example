# Copie para .env e ajuste se necessário (opcional — há valores padrão em setup.ts)
QA_EMAIL=seu.email@teddydigital.io
QA_PASSWORD=sua-senha

# PW_HEADLESS=true para rodar sem abrir o navegador
# PW_HEADLESS=true
